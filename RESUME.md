# 재개 노트

새 세션은 **이 파일만 읽고 시작할 수 있어야 한다.** 인수인계를 처음부터 재구성하는 데
여러 라운드를 쓴 적이 있어, 흩어진 운영 지식을 한 장으로 모았다.

---

## 현재 상태 (2026-07-30)

**통합 시뮬레이션 완성 — v1 §11 단계표 0~12 전체 완료.**
다음 작업은 시뮬이 아니라 **논문 쪽**이며, 출발점은 **v1.1 §14 (발견 요약)** 이다.

```
탭 1  영상법 단독          ✓
탭 2  도선 관 단독         ✓
탭 3  나란히 비교          ✓  (동결 — §10-1-3)
탭 4  (A) 지표 표 · (B) 분산 곡선 · (C) 수렴 스캔   ✓
게이트  G0·G1 PASS · G2·G3·G4 임계값 확정 · G5 보고   (node unified/verify.js)
```

논문 쪽 예정 작업: 표 5의 `a=0.5 mm` 재계산 · phasor-steps 그림 SVG 추출 · `FLOQUET_M` 정합.

---

## 읽는 순서

1. **이 파일**
2. **v1.1 §16** — 미검증 7건. 화면 확인이 남아 있다
3. **v1.1 §14** — 발견 요약 (논문 Discussion 재료)
4. **v1.1 §15** — 방법론 5항목
5. **v1 전문 · v1.1 전문** (충돌 시 **v1.1 우선**)

```
v1    waveguide_unified_spec_v1.md
v1.1  docs/superpowers/specs/2026-07-29-waveguide-unified-design.md
```

> ⚠ **절 번호가 v1 과 v1.1 에서 서로 다른 내용을 가리킨다.** 항상 어느 문서인지 확인할 것.
> 예: 색 매핑은 **v1 §9-1**(`### 9-1. 색 매핑 — 하나만`), **v1.1 §9-1** 은 프리셋이다.
> v1.1 §1 의 변경 표가 두 문서의 관계를 정리한다.

---

## 절대 규칙

- **`image-source/` · `line-wire/` 읽기 전용.** 원본 **17개** 파일(8 + 9) 무변경 유지.
  기계 검증: `git status --short -- image-source line-wire` 가 비어 있어야 한다.
  수정이 필요해 보이면 **고치지 말고 보고**한다 (v1 §1-1).
- **좌표·상수는 `unified/geometry.js` 에만**, **측정 코드는 `unified/measure.js` 에만** (v1 §1-2·§1-3).
  탭 4는 측정 함수를 **호출만** 한다.
<!-- TERMCHECK-EXCLUDE-START -->
- **용어 규약** — v1 §9 / v1 §1-5 + **v1.1 §1-1** (「상쇄」의 허용 용법 경계).
  금지어: 복사파(→산란파) · evanescent·증발장(→감쇠파) · z방향 "경로차" · 자기항(→자체 산란).
- **인과 방향** — `경계조건 → 유도 전류/산란파 → 장 결과`. 상쇄·차폐는 **결과이지 원인이 아니다.**
- **임의로 하위 결정하지 말 것** — v1 §14 / v1.1 §13.

### 용어 전수 점검 (기계 판독)

저장소 루트에서. v1.1 §1-1 에 같은 명령이 있다.

```sh
BAN='복사파|evanescent|증발장|경로차|자기항'
{ grep -rnE "$BAN" unified/ ;
  sed '/TERMCHECK-EXCLUDE-STAR[T]/,/TERMCHECK-EXCLUDE-EN[D]/d' \
      docs/superpowers/specs/2026-07-29-waveguide-unified-design.md | grep -nE "$BAN" ; }
echo '(위에 아무것도 없으면 0건 — 통과)'
```

`[T]`·`[D]` 대괄호를 **지우지 말 것** — 자기참조를 끊는 장치다 (이유는 v1.1 §1-1).
인과 역전은 `BAN='상쇄|차폐'` 로 바꿔 목록만 뽑고 **자동 판정하지 않는다** — 규약을 부정하는
문장(v1.1 §14-(4))도 걸리므로 사람이 본다.

> 점검 대상은 `unified/` 와 v1.1 문서다. **이 파일은 대상이 아니다.** 다만 여기서도 금지어를
> 인용하고 있어, 나중에 범위를 넓히더라도 자동 제외되도록 같은 `TERMCHECK-EXCLUDE` 표식으로
> 감싸 두었다.

<!-- TERMCHECK-EXCLUDE-END -->

---

## 페이지 여는 법 — 운영 지식 (재발견 비용이 컸다)

```
file:///C:/dev/03-task(waveguide%20unified)/unified/index.html?debug=1
```

- **폴더명에 공백이 있어 `%20` 이 필요하다.** 더블클릭으로는 `?debug=1` 이 붙지 않는다.
- **로컬 서버 불필요.** `file://` 직접 열기가 검증됐다 (v1.1 §3-1). ES 모듈·`fetch` 를 쓰지 않는다.
- **브라우저 줌 80 % 가 이 화면(하드웨어 DPR 1.25)의 올바른 설정**이다 — 실효 DPR 이 1.0 이 되어
  캔버스가 백킹 520×220 과 정확히 1:1 이 된다. 100 % 는 구조적으로 경계에 있다 (v1.1 §10-1-1).
- `?debug=1` 패널은 **우하단 고정 오버레이, 기본 접힘.** 배치에 영향을 주지 않는다.
- **성능은 사이드바 하단 `[측정 (5회 중앙값)]` 버튼으로만 판정한다.**
  `document.visibilityState !== 'visible'` 이면 숫자가 무의미하다 — 숨은 창은 Chrome 이
  렌더러 우선순위를 낮춰 **동기 계산도 7.5배 느려진다** (2549 → 340 ms 실측, v1.1 §10-1).
  그래서 버튼은 그 상태에서 측정을 거부한다.
- **Chrome 확장** — `/chrome` 으로 상태 확인. `file://` 페이지이므로 `chrome://extensions` 에서
  **"파일 URL에 대한 액세스 허용"** 이 켜져 있어야 한다.
- **에이전트가 띄운 창은 앞에 세울 수단이 없다.** 창은 가려지면 `visibilityState: hidden` 이 되고
  `rAF` 도 멈춘다 — **화면 확인과 성능 측정은 사람이 한다.**

### 검증 실행

```sh
node unified/verify.js                                            # 게이트 (약 75 s)
node -e "require('./unified/geometry.js').SCAT_BAND=false; require('./unified/verify.js');"
                                                                  # 벽 사이 제한 OFF 대조
```

---

## 알려진 함정

- **브라우저 줌은 Chrome 설정이라 확장·페이지 어느 쪽에서도 바꿀 수 없다.** 사람이 손으로 맞춘다.
- **`devicePixelRatio` 에 이미 줌이 곱해져 있다** (100 %→1.25, 80 %→1.0). 실효 DPR 은 이 값 **하나**이고,
  `outerWidth/innerWidth` 를 또 곱하면 줌을 두 번 센다 (v1.1 §10-1-1).
- **동결 목록 6건** (v1.1 §10-1-3) — 의도적으로 남긴 불완전이다. **발견해도 고치지 말 것.**
  2026-08-03 에 **라벨·설명·컨트롤 가시성만** 범위를 명시해 해제했다 (**v1.1 §10-1-3-a**).
  6건은 그대로 동결이다 — 해제 기록에 없는 것은 해제되지 않았다.
  그중 하나: `[N=∞ 정확 모드합]` 토글은 구현하지 않았고 탭 4 대조군으로 흡수됐다 (v1 §10-0 과의 차이).
- **자기가 바꾸는 값을 관찰하는 계산**은 판정 기준을 변경 전 상태로 고정한다 (v1.1 §10-1-2).
  이 함정은 `layout()` 의 세로 압축과 탭 4의 (C) 펼침에서 **두 번** 나왔다.
- **가드보다 계측이 먼저.** 루프가 아닌 곳에 가드를 넣으면 원인을 가린다 (v1.1 §15-3).

---

## 백업

원격이 없는 로컬 저장소다. 전 이력을 단일 파일로 보존한다.

```sh
git bundle create ../03-task-backup-YYYY-MM-DD.bundle --all      # 생성
git clone ../03-task-backup-YYYY-MM-DD.bundle <새폴더>            # 복원 (실제로 시험할 것)
```

`git bundle verify` 는 무결성만 본다. **`clone` 이 실제로 되는지가 백업의 값이다.**

> 이 저장소는 **`waveguide_a` 와 별개**다. 섞지 말 것. 원격을 연결하려면 **빈** 저장소를 만든 뒤
> `git remote add origin <URL>` → `git push -u origin master`.
